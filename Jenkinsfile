pipeline {
    agent any

    stages {
        stage('Install Dependencies') {
            steps {
                sh 'npm install'
            }
        }
        
    stages {
        stage('Test') {
            steps {
                echo 'Deploying'
            }
        }
    }
}